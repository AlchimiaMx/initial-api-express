const permissions = require('express-jwt-permissions')()
const Validate = require('../validate')

module.exports = (router, mongoose, config, upload ) => {
  //Models
  // const Example = require('./example/model') (mongoose)

  //functions
  //require('../example') (mongoose...)

  //Routers
  // require('./example')(router, permissions, Validate, Example, upload, config)

}
